name: Android CI

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. Grab your code
    - name: Checkout repo
      uses: actions/checkout@v3

    # 2. Set up JDK (match your project’s Java version)
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        distribution: temurin
        java-version: '11'
        cache: gradle

    # 3. Grant execute permissions to Gradle wrapper
    - name: Make gradlew executable
      run: chmod +x ./gradlew

    # 4. Build the APK (debug or release)
    - name: Build Debug APK
      run: ./gradlew assembleDebug --no-daemon

    # 5. Collect the APK as an artifact
    - name: Upload Debug APK
      uses: actions/upload-artifact@v3
      with:
        name: blackblub-debug-apk
        path: app/build/outputs/apk/debug/app-debug.apk

    # (Optional) If you want a signed release build:
    #- name: Build Release APK
    #  run: ./gradlew assembleRelease --no-daemon
    #- name: Upload Release APK
    #  uses: actions/upload-artifact@v3
    #  with:
    #    name: blackblub-release-apk
    #    path: app/build/outputs/apk/release/app-release.apk
